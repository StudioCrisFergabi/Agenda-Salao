rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Fotos da galeria: qualquer pessoa pode ver, só a proprietária
    // logada pode enviar ou apagar.
    match /gallery/{fileName} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
